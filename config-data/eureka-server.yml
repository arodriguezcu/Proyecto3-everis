server:
  port: 8761

eureka:
  instance:
#    hostname: localhost
#    hostname: 192.168.0.106
    hostname: https://docker-config-server.azurewebsites.net/
  client:
    fetch-registry: false
    register-with-eureka: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
