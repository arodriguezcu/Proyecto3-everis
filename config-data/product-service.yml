server:
  port: 8091
#  port: 80
  hostname: localhost
#  hostname: 192.168.0.106
  
spring:
  application:
    name: product-service
  data:
    mongodb:
      uri: mongodb://arodriguezcu:WoZiG7Enr4sZXJsPA6J1yAICIAeGAzczurFJNm8k3q0apmZjMRPlAdHKqoA4vCnXvwLT40VxnskXjU6AwznsuA==@arodriguezcu.mongo.cosmos.azure.com:10255/db_product?ssl=true&replicaSet=globaldb&retrywrites=false&maxIdleTimeMS=120000&appName=@arodriguezcu@
  kafka:
    bootstrap-servers: ${kafka.server.hostname}:${kafka.server.port}

kafka:
  server:
#    hostname: ${server.hostname}
    hostname: 20.94.153.173
    port: 9092
#    port: 29092
#    port: 80

eureka:
  client:
    service-url:
#      defaultZone: http://${eureka.instance.hostname}:8761/eureka
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
      defaultZone: http://${eureka.instance.hostname}:80/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
#    hostname: ${server.hostname}
    hostname: az-eureka-server.azurewebsites.net
            
msg:
  error:
    registro:
      notfound.all: PRODUCTOS NO ENCONTRADOS
      notfound: PRODUCTO NO IDENTIFICADO
      notfound.create: PRODUCTO NO SE PUDO CREAR
      if.exists: PRODUCTO YA EXISTE
      notfound.update: PRODUCTO NO IDENTIFICADO PARA ACTUALIZAR
      notfound.delete: CLIENTE NO IDENTIFICADO PARA ELIMINAR
      product.delete: CLIENTE ELIMINADO

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 2s
        failureRateThreshold: 50
        eventConsumerBufferSize: 10
        recordExceptions:
          - org.springframework.web.client.HttpServerErrorException
          - java.util.concurrent.TimeoutException
          - java.io.IOException
          - java.net.ConnectException
    instances:
      productServiceCircuitBreaker:
        baseConfig: default
